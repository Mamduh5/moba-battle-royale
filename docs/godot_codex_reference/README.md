# Deprecated Godot Codex Reference Pack

This folder is deprecated and must not be used as the active build contract.

Use the canonical entry point instead:

```text
docs/CODEX.md
```

For the one-prompt build challenge, start here:

```text
docs/godot_codex_reference_upgrade_v2/CODEX_ONE_PROMPT_BUILD.md
```

This folder is retained only as historical context from the first reference pack. New Codex work must follow:

```text
docs/godot_codex_reference_upgrade_v2/
```

Do not resolve conflicts by preferring files in this deprecated folder.
